---
//* IMPORTS
import '../styles/global.css';

import imgbackground from '../assets/bckgrnd_1.svg';
---

<div id="container" class="h-screen">
    <div id="background" class="relative h-full">
        <main id="main-base" class="h-full">
            <!-- Fondo de la imagen -->
            <img
                src={imgbackground.src}
                alt="Background"
                class="w-full h-full object-cover absolute top-0 left-0 z-0"
            />

            <!-- Contenido de texto y botón -->
            <div class="text-izq absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 text-center z-10 text-white px-4">
                <!-- Título -->
                <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold mb-4">
                    Descifrando el potencial
                </h1>

                <!-- Párrafo -->
                <p class="text-base sm:text-lg md:text-xl mb-8 max-w-2xl mx-auto">
                    Desde el desarrollo, diseño e implementación de sistemas electrónicos, hasta la planeación de multimedia. En Gradus Technologies desciframos el potencial en todas las áreas posibles.
                </p>

                <!-- Botón -->
                <button class="bg-white text-black px-6 py-2 sm:px-8 sm:py-3 font-semibold hover:bg-black hover:text-white transition-colors duration-300">
                    Más información
                </button>
            </div>
        </main>
    </div>
</div>